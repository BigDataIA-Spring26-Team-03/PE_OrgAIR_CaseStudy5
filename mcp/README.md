# MCP Server (moved)

The MCP server was moved to `pe_mcp/` to avoid shadowing the installed `mcp` SDK.

**Run the server:**
```bash
poetry run python -m pe_mcp.server
```

**Claude Desktop config:** Use `python -m pe_mcp.server` as the command (with appropriate args for your setup).
